export enum ErrorMessages {
  INTERNAL_SERVER_ERROR = 'An error occurred while processing your request',
}

export enum StreamingConfigurations {
  STREAMING_TRANSACTION_TOKEN = 'STREAMING_TRANSACTION',
  CLIENT_ID = 'transactions-microservice',
  GROUP_ID = 'transactions-producer',
}
